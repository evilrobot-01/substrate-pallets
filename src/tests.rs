use crate::mock::*;

#[test]
#[ignore]
fn test() {
	new_test_ext().execute_with(|| {
		// Started on mock integration, unfortunately no time to implement any tests for this pallet (as focused on the
		// dex pallet) :(
		todo!()
	});
}
